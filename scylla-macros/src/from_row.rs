use proc_macro::TokenStream;
use quote::{quote, quote_spanned};
use syn::{spanned::Spanned, DeriveInput};

// derive(FromRow) for the new FromRow trait
pub fn from_row_derive(tokens_input: TokenStream) -> TokenStream {
    unimplemented!()
}
